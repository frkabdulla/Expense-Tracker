@model IEnumerable<proj.Models.Expense>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Expns_Item)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Expns_Price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Item_Qty)
        </th>
        <th>
            <h6>Total</h6>
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Location)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Expns_Item)
        </td>
        @*<td>
            @Html.DisplayFor(modelItem => item.Expns_Price)

        </td>*@
        <td>

            @Html.DisplayFor(modelItem => item.Expns_Price)

        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Item_Qty)
        </td>
        <td>
            @Html.DisplayFor(ModelItem=>item.Item_Qty)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Location)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Expns_Item }) |
            @Html.ActionLink("Details", "Details", new { id = item.Expns_Item }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Expns_Item })
        </td>
    </tr>
}

</table>


<div>
    <h3>
       Total Expense is :😲 @Model.Sum(i => i.Expns_Price)
    </h3>
    <h3>

    </h3>
</div>
